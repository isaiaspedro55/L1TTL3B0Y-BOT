{"chorar":{"codigo":"async(ctx)=>{\n  const{sock,jid,seloBot,mencoes,sender}=ctx;\n  const alvo=mencoes[0]||sender;\n  await sock.sendMessage(jid,{\n    text:\"😭 \\n@\"+alvo.split(\"@\")[0]+\" está a chorar!\",\n    mentions:[alvo]\n  },{quoted:seloBot});\n}","autor":"L1TTL3B0Y💫","criadoEm":1788724641005},"chamar":{"codigo":"async(ctx)=>{\n  const{sock,jid,seloBot,isAdmin,isDono,args}=ctx; // <-- adicionei args aqui\n  \n  // 1. Só funciona em grupo\n  if(!jid.endsWith(\"@g.us\")){\n    await sock.sendMessage(jid,{text:\"❌ *Apenas em grupos.*\"}, {quoted:seloBot});\n    return;\n  }\n  \n  // 2. Só admin/dono pode usar\n  if(!isAdmin && !isDono){\n    await sock.sendMessage(jid,{text:\"❌ *Apenas administradores podem chamar!*\"}, {quoted:seloBot});\n    return;\n  }\n  \n  try{\n    // 3. Pega todos os membros do grupo\n    const metadata = await sock.groupMetadata(jid);\n    const membros = metadata.participants.map(p => p.id);\n    \n    // 4. Pega o motivo da chamada\n    const motivo = args.join(' ') || 'Chamada de voz no grupo'; // <-- corrigido\n    \n    // 5. Monta a mensagem marcando todo mundo\n    const texto = `📞 *CHAMADA DE VOZ INICIADA!* 📞\\n\\n*Motivo:* ${motivo}\\n\\n⚠️ *Entrem na chamada agora!*\\nToquem no ícone de telefone aqui em cima 👆`;\n    \n    await sock.sendMessage(jid,{\n      text: texto,\n      mentions: membros // marca @todos\n    }, {quoted:seloBot});\n    \n  }catch(e){\n    await sock.sendMessage(jid,{text:`❌ *Erro ao chamar:* ${e.message}`}, {quoted:seloBot});\n  }\n}","autor":"L1TTL3B0Y💫","criadoEm":1788731228870},"p":{"codigo":"async(ctx)=>{const{sock,jid,seloBot,args,fs,CONFIG}=ctx;if(!args[0]){const msgPref=`*${CONFIG.PREFIXO}*`;try{await sock.sendMessage(jid,{text:`📌 *Prefixo Atual:* ${msgPref}\\n\\nUse: *${CONFIG.PREFIXO}prefixo!* pra trocar`,footer:`Prefixo do ${CONFIG.NOME_BOT}`,buttons:[{buttonId:`use_prefix_${CONFIG.PREFIXO}`,buttonText:{displayText:`『 ${CONFIG.PREFIXO} 』`},type:1}],headerType:1},{quoted:seloBot})}catch{await sock.sendMessage(jid,{text:`📌 *Prefixo Atual:* ${msgPref}`},{quoted:seloBot})}return}const novoPref=args[0].trim().charAt(0);CONFIG.PREFIXO=novoPref;try{const configPath='./config.json';let config=JSON.parse(fs.readFileSync(configPath));config.PREFIXO=novoPref;fs.writeFileSync(configPath,JSON.stringify(config,null,2))}catch(e){}await sock.sendMessage(jid,{text:`✅ *Prefixo alterado para* *${novoPref}*\\n\\nExemplo: *${novoPref}menu*`},{quoted:seloBot})}","autor":"L1TTL3B0Y💫","criadoEm":1789234459917}}
