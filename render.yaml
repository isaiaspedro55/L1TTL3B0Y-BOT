services:
  - type: web
    name: l1ttl3b0y
    env: node
    plan: free

    buildCommand: |
      npm install
      pip install yt-dlp edge-tts --break-system-packages

    startCommand: node index.js

    envVars:
      - key: NODE_ENV
        value: production
      - key: TZ
        value: Africa/Luanda
